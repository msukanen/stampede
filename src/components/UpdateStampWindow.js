import { Container } from 'react-bootstrap'

export const UpdateStampWindow = (props) => {
    return(
        <Container>
            Lets add/update a stamp!
        </Container>
    )
}

export default UpdateStampWindow
